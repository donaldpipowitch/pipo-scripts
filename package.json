{
  "private": true,
  "scripts": {
    "release": "workspace-version",
    "build": "yarn wsrun build --serial --exclude pipo-scripts"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "workspace-release": "^0.4.3",
    "wsrun": "^1.3.0"
  }
}
