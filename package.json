{
  "private": true,
  "scripts": {
    "release": "workspace-version",
    "build": "yarn wsrun build --serial --exclude-missing",
    "start": "yarn wsrun start --exclude-missing"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "workspace-release": "^0.4.3",
    "wsrun": "^2.0.0"
  }
}
